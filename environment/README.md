# Environment files

The supplied YAML and requirements files describe the reported versions but have not been solved or validated in this generated package. Replace them with exports from the actual working environments used for the final analyses. This is especially important for transitive dependencies such as NumPy, pandas, SciPy, AnnData, and Bioconductor packages.
